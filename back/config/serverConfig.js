// serverConfig.js

const express = require('express');

function createServer() {
  const app = express();
  
  // Aqui você pode definir as rotas e middlewares do seu aplicativo
  
  return app;
}

module.exports = createServer;
